'use strict';'require rpc';'require uci';'require chester.ui as chester';const callHostHints=rpc.declare({object:'luci-rpc',method:'getHostHints',expect:{}});const callWirelessDevices=rpc.declare({object:'luci-rpc',method:'getWirelessDevices',expect:{}});const callAssoclist=rpc.declare({object:'iwinfo',method:'assoclist',params:['device'],expect:{results:[]}});const callNeighbors=rpc.declare({object:'chester',method:'neighbors',params:['devices'],expect:{neighbors:[]}});const callDhcpLeases=rpc.declare({object:'chester',method:'dhcp_leases',expect:{leases:[]}});function normalizeHostHints(hostHints){const normalized={};Object.keys(hostHints||{}).forEach((mac)=>{normalized[chester.normalizeMac(mac)]=hostHints[mac]||{};});return normalized;}
function pickList(value,key){if(Array.isArray(value))
return value;if(L.isObject(value)&&Array.isArray(value[key]))
return value[key];return[];}
function normalizeEntries(value){if(Array.isArray(value))
return value;if(L.isObject(value))
return Object.keys(value).map((key)=>value[key]);return[];}
function isTruthyFlag(value){return value===true||value===1||value==='1'||value==='true'||value==='yes'||value==='on';}
function isFalseFlag(value){return value===false||value===0||value==='0'||value==='false'||value==='no'||value==='off';}
function isNeighborOnline(neighbor){if(!neighbor)
return false;if(isTruthyFlag(neighbor.online))
return true;if(isFalseFlag(neighbor.online))
return false;return['FAILED','INCOMPLETE','NOARP'].indexOf(String(neighbor.state||'').toUpperCase())===-1;}
function formatLeaseTime(seconds){if(seconds==null)
return'--';seconds=Math.max(0,Number(seconds)||0);const days=Math.floor(seconds/86400);const hours=Math.floor((seconds%86400)/3600);const minutes=Math.floor((seconds%3600)/60);if(days>0)
return hours>0?_('%dd %dh').format(days,hours):_('%dd').format(days);if(hours>0)
return minutes>0?_('%dh %dm').format(hours,minutes):_('%dh').format(hours);return _('%dm').format(minutes);}
function buildDhcpLeaseMap(leases){const leaseMap={};normalizeEntries(leases).forEach((lease)=>{const mac=chester.normalizeMac(lease.macaddr);if(!mac)
return;leaseMap[mac]={expires:lease.expires,remaining:lease.remaining,time:formatLeaseTime(lease.remaining),ipaddr:lease.ipaddr,hostname:lease.hostname};});return leaseMap;}
function bandLabel(radio){switch(radio?.config?.band){case'2g':return'2.4 GHz';case'5g':return'5 GHz';case'6g':return'6 GHz';default:return _('Wireless');}}
return L.Class.extend({getWiredInterfaces:function(){const value=uci.get('chester','clients','wired_interfaces');if(Array.isArray(value)&&value.length)
return value;if(value)
return[value];return['br-lan'];},buildStaticLeaseMap:function(){const leases={};try{uci.sections('dhcp','host').forEach((section)=>{chester.ensureArray(section.mac).forEach((mac)=>{leases[chester.normalizeMac(mac)]={section:section['.name'],name:section.name,ip:section.ip,interface:section.interface};});});}
catch(err){}
return leases;},fetchRuntimeData:async function(){const wiredInterfaces=this.getWiredInterfaces();const[hostHints,wirelessData,neighbors,dhcpLeases]=await Promise.all([L.resolveDefault(callHostHints(),{}),L.resolveDefault(callWirelessDevices(),{}),L.resolveDefault(callNeighbors(wiredInterfaces.join(',')),[]),L.resolveDefault(callDhcpLeases(),[])]);const assocTasks=[];const radioByIfname={};Object.keys(wirelessData||{}).forEach((radioName)=>{const radio=wirelessData[radioName];chester.ensureArray(radio?.interfaces).forEach((iface)=>{if(!iface?.ifname)
return;radioByIfname[iface.ifname]={radioName:radioName,bandLabel:bandLabel(radio),ssid:iface?.config?.ssid||radio?.iwinfo?.ssid||'',ifname:iface.ifname};assocTasks.push(L.resolveDefault(callAssoclist(iface.ifname),[]).then((result)=>({ifname:iface.ifname,results:pickList(result,'results')})));});});return{hostHints:hostHints,wirelessData:wirelessData,wiredInterfaces:wiredInterfaces,radioByIfname:radioByIfname,assoclists:await Promise.all(assocTasks),neighbors:pickList(neighbors,'neighbors'),dhcpLeaseMap:buildDhcpLeaseMap(pickList(dhcpLeases,'leases'))};},buildWirelessMap:function(data){const wirelessMap={};normalizeEntries(data.assoclists).forEach((entry)=>{const meta=data.radioByIfname[entry.ifname];normalizeEntries(entry.results).forEach((station)=>{const mac=chester.normalizeMac(station.mac);wirelessMap[mac]={connection:'wireless',band:meta?.bandLabel||_('Wireless'),ssid:meta?.ssid||'--',ifname:entry.ifname,signal:station.signal!=null?`${station.signal} dBm`:'--',rxRate:chester.rateText(station.rx),txRate:chester.rateText(station.tx)};});});return wirelessMap;},buildDevices:function(data,options){const opts=options||{};const hostHints=normalizeHostHints(data.hostHints);const staticLeases=opts.staticLeases||{};const includeOfflineStatic=opts.includeOfflineStatic!==false;const wirelessMap=this.buildWirelessMap(data);const dhcpLeaseMap=data.dhcpLeaseMap||{};const devices={};Object.keys(wirelessMap).forEach((mac)=>{const hint=hostHints[mac]||{};const dhcpLease=dhcpLeaseMap[mac]||null;const ipv4=dhcpLease?.ipaddr||chester.ensureArray(hint.ipaddrs)[0]||'--';devices[mac]={mac:mac,hostname:hint.name||'--',ipaddr:ipv4,connection:'wireless',interface:wirelessMap[mac].ifname,band:wirelessMap[mac].band,ssid:wirelessMap[mac].ssid,signal:wirelessMap[mac].signal,rxRate:wirelessMap[mac].rxRate,txRate:wirelessMap[mac].txRate,tone:'online',state:'ASSOCIATED',staticLease:staticLeases[mac]||null,dhcpLease:dhcpLease};});normalizeEntries(data.neighbors).forEach((neighbor)=>{if(!neighbor?.macaddr||!isNeighborOnline(neighbor)||String(neighbor.family||'').toLowerCase()!=='ipv4')
return;const mac=chester.normalizeMac(neighbor.macaddr);const hint=hostHints[mac]||{};const dhcpLease=dhcpLeaseMap[mac]||null;if(devices[mac]){devices[mac].ipaddr=dhcpLease?.ipaddr||neighbor.ipaddr||devices[mac].ipaddr;devices[mac].dhcpLease=dhcpLease||devices[mac].dhcpLease||null;devices[mac].tone='online';devices[mac].state=neighbor.state||devices[mac].state;return;}
devices[mac]={mac:mac,hostname:hint.name||'--',ipaddr:dhcpLease?.ipaddr||neighbor.ipaddr,connection:'wired',interface:neighbor.device,band:'--',ssid:'--',signal:'--',rxRate:'--',txRate:'--',tone:'online',state:neighbor.state,staticLease:staticLeases[mac]||null,dhcpLease:dhcpLease};});if(includeOfflineStatic){Object.keys(staticLeases).forEach((mac)=>{const lease=staticLeases[mac];const hint=hostHints[mac]||{};if(devices[mac]){devices[mac].staticLease=lease;devices[mac].dhcpLease=dhcpLeaseMap[mac]||devices[mac].dhcpLease||null;if(!chester.hasValue(devices[mac].hostname)&&lease.name)
devices[mac].hostname=lease.name;if(!chester.hasValue(devices[mac].ipaddr)||devices[mac].ipaddr==='--')
devices[mac].ipaddr=lease.ip||devices[mac].ipaddr;return;}
devices[mac]={mac:mac,hostname:lease.name||hint.name||'--',ipaddr:lease.ip||chester.ensureArray(hint.ipaddrs)[0]||'--',connection:'static',interface:lease.interface||'lan',band:'--',ssid:'--',signal:'--',rxRate:'--',txRate:'--',tone:'offline',state:'OFFLINE',staticLease:lease,dhcpLease:dhcpLeaseMap[mac]||null};});}
return Object.keys(devices).sort((left,right)=>{const a=devices[left];const b=devices[right];const aOnline=a.tone==='online'?1:0;const bOnline=b.tone==='online'?1:0;if(aOnline!==bOnline)
return bOnline-aOnline;if(!!a.staticLease!==!!b.staticLease)
return a.staticLease?-1:1;return L.naturalCompare(a.hostname==='--'?a.mac:a.hostname,b.hostname==='--'?b.mac:b.hostname);}).map((mac)=>devices[mac]);},countOnlineDevices:function(data,options){return this.buildDevices(data,Object.assign({includeOfflineStatic:false},options||{})).filter((device)=>device.tone==='online').length;}});