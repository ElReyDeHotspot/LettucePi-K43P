'use strict';'require rpc';'require uci';const FIELD_GROUPS=['Base Information','SIM Information','Network Info','Cell Info'];const GROUP_LABELS={baseinformation:'Base Information',siminformation:'SIM Information',networkinformation:'Network Info',cellinformation:'Cell Info'};const ENTRY_LABELS={apn:'APN',arfcn:'ARFCN',atport:'AT Port',auth:'Authentication',accessmaximumbitratefordownlink:'Downlink AMBR',accessmaximumbitrateforuplink:'Uplink AMBR',band:'Band',bands:'Band',cardmode:'SIM Status',carrier:'Carrier',cellid:'Cell ID',cellidentifier:'Cell ID',channelqualityindicatorfordownlink:'Downlink CQI',channelqualityindicatorforuplink:'Uplink CQI',configsection:'Config Section',connectionstatus:'Connection Status',connectstatus:'Connection Status',connectedtype:'Network Type',devicemodel:'Device Model',dlbandwidth:'DL Bandwidth',dns1:'DNS 1',dns2:'DNS 2',duplexmode:'Duplex Mode',firmwareversion:'Firmware Version',gateway:'Gateway',iccid:'ICCID',imei:'IMEI',imsi:'IMSI',interface:'Interface',internetserviceprovider:'Carrier',ipaddress:'IP Address',ipv4:'IPv4',ipv4address:'IPv4',ipv6:'IPv6',ipv6address:'IPv6',linkdevice:'Link Device',logicalinterface:'Network Port',manufacturer:'Manufacturer',model:'Device Model',mobilecountrycode:'MCC',mobilenetworkcode:'MNC',modulemodel:'Device Model',name:'Name',networkinterface:'Network Port',networkmode:'Network Type',networkoperator:'Carrier',networktype:'Network Type',operator:'Carrier',physicalcellid:'PCI',pci:'PCI',productmodel:'Device Model',protocol:'Protocol',revision:'Revision',rsrp:'RSRP',rsrq:'RSRQ',rssi:'RSSI',serviceprovider:'Carrier',servicetype:'Network Type',servingcellreceivelevel:'Serving Cell Receive Level',simcardstatus:'SIM Status',simslot:'SIM Slot',simstatus:'SIM Status',sinr:'SINR',spn:'Carrier',status:'Status',temperature:'Temperature',trackingareacode:'Tracking Area Code',ulbandwidth:'UL Bandwidth',voltage:'Voltage',uptime:'Online Time'};const VALUE_LABELS={no:'No',ready:'Ready',yes:'Yes'};const callBaseInfo=rpc.declare({object:'qmodem',method:'base_info',params:['config_section'],expect:{}});const callNetworkInfo=rpc.declare({object:'qmodem',method:'network_info',params:['config_section'],expect:{}});const callCellInfo=rpc.declare({object:'qmodem',method:'cell_info',params:['config_section'],expect:{}});const callSimInfo=rpc.declare({object:'qmodem',method:'sim_info',params:['config_section'],expect:{}});const callInfo=rpc.declare({object:'qmodem',method:'info',params:['config_section'],expect:{}});const callConnectStatus=rpc.declare({object:'qmodem',method:'get_connect_status',params:['config_section'],expect:{}});const callDialStatus=rpc.declare({object:'qmodem',method:'dial_status',params:['config_section'],expect:{}});const callGetAtCfg=rpc.declare({object:'qmodem',method:'get_at_cfg',params:['config_section'],expect:{}});const callGetImei=rpc.declare({object:'qmodem',method:'get_imei',params:['config_section'],expect:{}});const callSendAt=rpc.declare({object:'qmodem',method:'send_at',params:['config_section','params'],expect:{}});const callSetImei=rpc.declare({object:'qmodem',method:'set_imei',params:['config_section','imei'],expect:{}});const callGetDisabledFeatures=rpc.declare({object:'qmodem',method:'get_disabled_features',params:['config_section'],expect:{}});const callGetMode=rpc.declare({object:'qmodem',method:'get_mode',params:['config_section'],expect:{}});const callSetMode=rpc.declare({object:'qmodem',method:'set_mode',params:['config_section','mode'],expect:{}});const callGetNetworkPrefer=rpc.declare({object:'qmodem',method:'get_network_prefer',params:['config_section'],expect:{}});const callSetNetworkPrefer=rpc.declare({object:'qmodem',method:'set_network_prefer',params:['config_section','params'],expect:{}});const callGetLockBand=rpc.declare({object:'qmodem',method:'get_lockband',params:['config_section'],expect:{}});const callSetLockBand=rpc.declare({object:'qmodem',method:'set_lockband',params:['config_section','params'],expect:{}});const callGetNeighborCell=rpc.declare({object:'qmodem',method:'get_neighborcell',params:['config_section'],expect:{}});const callSetNeighborCell=rpc.declare({object:'qmodem',method:'set_neighborcell',params:['config_section','params'],expect:{}});const callGetRebootCaps=rpc.declare({object:'qmodem',method:'get_reboot_caps',params:['config_section'],expect:{}});const callDoReboot=rpc.declare({object:'qmodem',method:'do_reboot',params:['config_section','params'],expect:{}});const callRestartModemDial=rpc.declare({object:'chester',method:'restart_modem_dial',expect:{}});const callModemBoostInfo=rpc.declare({object:'chester',method:'modem_boost_info',expect:{}});const callModemBoostApply=rpc.declare({object:'chester',method:'modem_boost_apply',params:['mode','value'],expect:{}});const callScanPcie=rpc.declare({object:'qmodem',method:'scan_pcie',expect:{}});const callScanUsb=rpc.declare({object:'qmodem',method:'scan_usb',expect:{}});const callScanAll=rpc.declare({object:'qmodem',method:'scan_all',expect:{}});const callGetNetworkInterfaces=rpc.declare({object:'qmodem',method:'get_network_interfaces',expect:{interfaces:[]}});const callNetworkDevicesStatusMap=rpc.declare({object:'network.device',method:'status',expect:{'':{}}});const callGetSimSwitchCapabilities=rpc.declare({object:'qmodem',method:'get_sim_switch_capabilities',params:['config_section'],expect:{}});const callGetSimSlot=rpc.declare({object:'qmodem',method:'get_sim_slot',params:['config_section'],expect:{}});const callSetSimSlot=rpc.declare({object:'qmodem',method:'set_sim_slot',params:['config_section','slot'],expect:{}});const callTrafficStats=rpc.declare({object:'qmodem',method:'get_stats',params:['config_section'],expect:{}});const callClearTraffic=rpc.declare({object:'qmodem',method:'clear_stats',params:['config_section'],expect:{}});function normalizeKey(value){return String(value||'').toLowerCase().replace(/[^a-z0-9]+/g,'');}
function humanizeLabel(value,fallback){const source=String(value||'').trim();if(!source)
return fallback||_('Details');return source.replace(/[_-]+/g,' ').replace(/([a-z0-9])([A-Z])/g,'$1 $2').replace(/\s+/g,' ').replace(/\b\w/g,(ch)=>ch.toUpperCase());}
function hasAsciiLabel(value){return/[A-Za-z]/.test(String(value||''));}
function resolveMappedLabel(value,mapping){const mapped=mapping[normalizeKey(value)];return mapped?_(mapped):null;}
function modemLabel(section){if(!section)
return'--';const name=section.name||section.id||'--';return section.alias?`${section.alias} (${name})`:name;}
function boolValue(value,fallback){if(value==null||value==='')
return fallback===true;return value!=='0'&&value!=='false'&&value!==false;}
function flattenEntries(result){if(Array.isArray(result))
return result;if(Array.isArray(result?.modem_info))
return result.modem_info;return[];}
function pushUnique(list,seen,value){if(!value||seen[value])
return;seen[value]=true;list.push(value);}
function expectSuccessCode(result,fallbackMessage){const code=Array.isArray(result)?Number(result[0]):Number(result?.code);if(!Number.isFinite(code)||code!==0){const err=new Error(result?.message||(Array.isArray(result)?'RPC call failed with status %d'.format(code):null)||fallbackMessage||'Command failed');err.details=result||{};throw err;}
return result;}
function asDisplayValue(value,fallback){if(value==null||value==='')
return fallback||'--';return String(value);}
function normalizeInterfaceDump(dump){if(Array.isArray(dump))
return dump;if(Array.isArray(dump?.interface))
return dump.interface;return[];}
function findLogicalInterfaceInfo(dump,name){if(!name)
return null;return normalizeInterfaceDump(dump).find((item)=>item?.interface===name)||null;}
function formatIpList(list){const values=Array.isArray(list)?list:[];const formatted=values.map((entry)=>{const address=String(entry?.address||'').trim();const mask=entry?.mask;if(!address)
return null;return mask!=null&&mask!==''?`${address}/${mask}`:address;}).filter((value)=>!!value);return formatted.length?formatted.join(', '):'--';}
function isIpv6Address(value){return String(value||'').indexOf(':')!==-1;}
function formatDnsByFamily(list,family){const values=[];const seen={};(Array.isArray(list)?list:[]).forEach((entry)=>{const value=String(entry||'').trim();if(!value)
return;if((family==='ipv6')!==isIpv6Address(value))
return;if(seen[value])
return;seen[value]=true;values.push(value);});return values.length?values.join(', '):'--';}
function formatGatewayByFamily(route,family){const values=Array.isArray(route)?route:[];const match=values.find((entry)=>{const target=String(entry?.target||'').trim();const mask=Number(entry?.mask);if(family==='ipv4')
return target==='0.0.0.0/0'||(target==='0.0.0.0'&&mask===0);return target==='::/0'||target==='default'||(target==='::'&&mask===0);});return asDisplayValue(match?.nexthop,'--');}
function formatDuration(totalSeconds){if(totalSeconds==null||isNaN(totalSeconds))
return'--';let remaining=Math.max(0,Math.floor(totalSeconds));const days=Math.floor(remaining/86400);remaining%=86400;const hours=Math.floor(remaining/3600);remaining%=3600;const minutes=Math.floor(remaining/60);const seconds=remaining%60;const parts=[];if(days)
parts.push(`${days}d`);if(hours||parts.length)
parts.push(`${hours}h`);if(minutes||parts.length)
parts.push(`${minutes}m`);parts.push(`${seconds}s`);return parts.join(' ');}
function interfaceTone(info){if(!L.isObject(info))
return'offline';if(info.pending===true)
return'warn';return info.up===true?'online':'offline';}
function interfaceText(info){if(!L.isObject(info))
return _('Disconnected');if(info.pending===true)
return _('Idle');return info.up===true?_('Connected'):_('Disconnected');}
function getNetworkInterfaceSections(){try{return uci.sections('network','interface');}
catch(err){return[];}}
function getModemConfigSection(section){return String(section?.id||section?.configSection||section?.['.name']||'').trim();}
function getModemBaseName(section){const alias=String(section?.alias||'').trim();return alias||getModemConfigSection(section);}
function getBoundInterfaces(configSection){return getNetworkInterfaceSections().filter((section)=>String(section?.modem_config||'').trim()===configSection).map((section)=>({name:String(section?.['.name']||'').trim(),proto:String(section?.proto||'').trim()})).filter((section)=>!!section.name);}
function pickBaseInterface(boundInterfaces,baseName,ipv6Name){if(boundInterfaces.some((section)=>section.name===baseName))
return baseName;const primary=boundInterfaces.find((section)=>section.name!==ipv6Name&&section.proto!=='dhcpv6');if(primary)
return primary.name;const fallback=boundInterfaces.find((section)=>section.name!==ipv6Name);return fallback?.name||null;}
function pickIpv6Interface(boundInterfaces,ipv6Name){if(boundInterfaces.some((section)=>section.name===ipv6Name))
return ipv6Name;const fallback=boundInterfaces.find((section)=>section.proto==='dhcpv6');return fallback?.name||null;}
function resolveExactInterfaceName(dump,name){return findLogicalInterfaceInfo(dump,name)?name:null;}
function interfaceDisplayName(primary,fallback){return asDisplayValue(primary||fallback,'--');}
return L.Class.extend({fieldGroups:FIELD_GROUPS,boolValue,flattenEntries,humanizeLabel,modemLabel,findEntry(entries,patterns,fallback){const normalized=(Array.isArray(patterns)?patterns:[patterns]).map((pattern)=>normalizeKey(pattern));const match=(Array.isArray(entries)?entries:[]).find((entry)=>normalized.indexOf(normalizeKey(entry?.key))!==-1);return match?.value!=null&&match?.value!==''?match.value:(fallback||'--');},resolveGroupTitle(group){return resolveMappedLabel(group,GROUP_LABELS)||humanizeLabel(group,_('Details'));},resolveEntryLabel(entry){const fullName=entry?.full_name;const key=entry?.key;const mapped=resolveMappedLabel(key,ENTRY_LABELS)||resolveMappedLabel(fullName,ENTRY_LABELS);const fallbackSource=hasAsciiLabel(fullName)?fullName:key;return mapped||humanizeLabel(fallbackSource,_('Details'));},resolveEntryValue(entry){const rawValue=entry?.value;const rawText=String(rawValue==null?'':rawValue).trim();const fieldKey=normalizeKey(entry?.key||entry?.full_name);if(!rawText)
return'--';if(fieldKey==='connectstatus'||fieldKey==='connectionstatus'){if(normalizeKey(rawText)==='yes')
return _('Connected');if(normalizeKey(rawText)==='no')
return _('Disconnected');}
const mapped=resolveMappedLabel(rawText,VALUE_LABELS);return mapped||rawValue;},groupEntries(results){const entries=[].concat(flattenEntries(results?.baseInfo)).concat(flattenEntries(results?.simInfo)).concat(flattenEntries(results?.networkInfo)).concat(flattenEntries(results?.cellInfo)).concat(flattenEntries(results?.info));const groups={};const seenEntries={};FIELD_GROUPS.forEach((group)=>{groups[group]=[];seenEntries[group]={};});entries.forEach((entry)=>{const group=entry?.class_origin||entry?.class||'Base Information';const label=this.resolveEntryLabel(entry)||_('Warning');const value=this.resolveEntryValue(entry);const warning=entry?.type==='warning_message';if(!groups[group]){groups[group]=[];seenEntries[group]={};}
const dedupeKey=`${normalizeKey(label)}\u0000${String(value)}\u0000${warning ? '1' : '0'}`;if(seenEntries[group][dedupeKey])
return;seenEntries[group][dedupeKey]=true;if(warning){groups[group].push({label,value,warning:true});return;}
if(!entry?.full_name&&!entry?.key)
return;groups[group].push({label,value});});return groups;},getModemSections(){try{return uci.sections('qmodem','modem-device').filter((section)=>{const state=String(section.state||section.status||'').toLowerCase();return state!=='disabled'&&section.disabled!=='1';}).map((section)=>({id:section['.name'],name:section.name||section['.name'],alias:section.alias||'',network:section.network||'',atPort:section.at_port||'',enabled:section.enable_dial!=='0',useUbus:section.use_ubus==='1'}));}
catch(err){return[];}},async loadModems(){await uci.load('qmodem');return this.getModemSections();},ensureSelected(selectedId,sections){const list=Array.isArray(sections)?sections:[];return list.some((section)=>section.id===selectedId)?selectedId:list[0]?.id||null;},async getNetworkInterfaces(){const result=await L.resolveDefault(callGetNetworkInterfaces(),{interfaces:[]});return Array.isArray(result?.interfaces)?result.interfaces:[];},resolveModemInterfaceInfo(section,interfaceDump){const configSection=getModemConfigSection(section);const baseName=getModemBaseName(section);const derivedIpv6Name=baseName?`${baseName}v6`:'';const boundInterfaces=configSection?getBoundInterfaces(configSection):[];let baseInterfaceName=pickBaseInterface(boundInterfaces,baseName,derivedIpv6Name);let ipv6InterfaceName=pickIpv6Interface(boundInterfaces,derivedIpv6Name);if(!baseInterfaceName)
baseInterfaceName=resolveExactInterfaceName(interfaceDump,baseName);if(!ipv6InterfaceName)
ipv6InterfaceName=resolveExactInterfaceName(interfaceDump,derivedIpv6Name);const baseInfo=findLogicalInterfaceInfo(interfaceDump,baseInterfaceName);const resolvedIpv6Info=findLogicalInterfaceInfo(interfaceDump,ipv6InterfaceName);const ipv6Info=resolvedIpv6Info||baseInfo;const primaryInfo=baseInfo||resolvedIpv6Info;const ipv6SourceIsBase=!resolvedIpv6Info&&formatIpList(baseInfo?.['ipv6-address'])!=='--';const resolvedBaseInterface=interfaceDisplayName(baseInterfaceName,baseName);const resolvedIpv6Interface=interfaceDisplayName(ipv6InterfaceName||(ipv6SourceIsBase?baseInterfaceName:''),derivedIpv6Name);return{baseInterface:resolvedBaseInterface,ipv6Interface:resolvedIpv6Interface,proto:asDisplayValue(primaryInfo?.proto,'--'),l3Device:asDisplayValue(primaryInfo?.l3_device||primaryInfo?.device,'--'),statusText:interfaceText(primaryInfo),tone:interfaceTone(primaryInfo),uptime:formatDuration(primaryInfo?.uptime),ipv4:formatIpList(baseInfo?.['ipv4-address']),ipv4Gateway:formatGatewayByFamily(baseInfo?.route,'ipv4'),ipv4Dns:formatDnsByFamily(baseInfo?.['dns-server'],'ipv4'),ipv6:formatIpList(ipv6Info?.['ipv6-address']),ipv6Gateway:formatGatewayByFamily(ipv6Info?.route,'ipv6'),ipv6Dns:formatDnsByFamily(ipv6Info?.['dns-server'],'ipv6')};},async getLanDevices(currentValue){const result=await L.resolveDefault(callNetworkDevicesStatusMap(),{});const seen={};const names=[];Object.keys(result||{}).forEach((name)=>{if(/^lan/i.test(name))
pushUnique(names,seen,name);});pushUnique(names,seen,currentValue);return names.sort((left,right)=>L.naturalCompare(left,right));},async fetchInfoBundle(sectionId){const[baseInfo,networkInfo,cellInfo,simInfo,info,connectStatus,dialStatus]=await Promise.all([L.resolveDefault(callBaseInfo(sectionId),{}),L.resolveDefault(callNetworkInfo(sectionId),{}),L.resolveDefault(callCellInfo(sectionId),{}),L.resolveDefault(callSimInfo(sectionId),{}),L.resolveDefault(callInfo(sectionId),{}),L.resolveDefault(callConnectStatus(sectionId),{}),L.resolveDefault(callDialStatus(sectionId),{})]);return{baseInfo,networkInfo,cellInfo,simInfo,info,connectStatus,dialStatus};},getAtCfg(sectionId){return L.resolveDefault(callGetAtCfg(sectionId),{});},getImei(sectionId){return L.resolveDefault(callGetImei(sectionId),{});},sendAt(sectionId,port,command,useUbus){const params={port:port,at:command};if(useUbus!=null)
params.use_ubus=useUbus?'1':'0';return L.resolveDefault(callSendAt(sectionId,params),{});},setImei(sectionId,imei){return L.resolveDefault(callSetImei(sectionId,imei),{});},getDisabledFeatures(sectionId){return L.resolveDefault(callGetDisabledFeatures(sectionId),{});},getMode(sectionId){return L.resolveDefault(callGetMode(sectionId),{});},setMode(sectionId,mode){return L.resolveDefault(callSetMode(sectionId,mode),{});},getNetworkPrefer(sectionId){return L.resolveDefault(callGetNetworkPrefer(sectionId),{});},setNetworkPrefer(sectionId,params){return L.resolveDefault(callSetNetworkPrefer(sectionId,params),{});},getLockBand(sectionId){return L.resolveDefault(callGetLockBand(sectionId),{});},setLockBand(sectionId,params){return L.resolveDefault(callSetLockBand(sectionId,params),{});},getNeighborCell(sectionId){return L.resolveDefault(callGetNeighborCell(sectionId),{});},setNeighborCell(sectionId,params){return L.resolveDefault(callSetNeighborCell(sectionId,params),{});},getRebootCaps(sectionId){return L.resolveDefault(callGetRebootCaps(sectionId),{});},doReboot(sectionId,method){return L.resolveDefault(callDoReboot(sectionId,{method:method||'soft'}),{});},async restartDialService(){const result=await callRestartModemDial();if(result?.result!==true){const err=new Error(result?.message||'qmodem_network restart failed');err.details=result||{};throw err;}
return result;},getBoostInfo(){return L.resolveDefault(callModemBoostInfo(),{});},async applyBoost(mode,value){return expectSuccessCode(await callModemBoostApply(mode,value),'Failed to save boost settings');},async scanPcie(){return expectSuccessCode(await callScanPcie(),'PCIe scan failed');},async scanUsb(){return expectSuccessCode(await callScanUsb(),'USB scan failed');},async scanAll(){return expectSuccessCode(await callScanAll(),'Full scan failed');},getSimSwitchCapabilities(sectionId){return L.resolveDefault(callGetSimSwitchCapabilities(sectionId),{});},getSimSlot(sectionId){return L.resolveDefault(callGetSimSlot(sectionId),{});},setSimSlot(sectionId,slot){return L.resolveDefault(callSetSimSlot(sectionId,slot),{});},getTrafficStats(sectionId){return L.resolveDefault(callTrafficStats(sectionId),{});},clearTrafficStats(sectionId){return L.resolveDefault(callClearTraffic(sectionId),{});}});