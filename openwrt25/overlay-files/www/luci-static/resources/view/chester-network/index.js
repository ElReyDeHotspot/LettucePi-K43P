'use strict';'require view';'require rpc';'require uci';'require ui';'require dom';'require chester.ui as chester';const callBuiltinEthernetPorts=rpc.declare({object:'luci',method:'getBuiltinEthernetPorts',expect:{result:[]}});function arrayValue(value){if(Array.isArray(value))
return value;if(value==null||value==='')
return[];return[value];}
function wordListValue(value){const values=Array.isArray(value)?value:value==null?[]:[value];const result=[];values.forEach((entry)=>{String(entry||'').trim().split(/\s+/).forEach((word)=>{if(word&&result.indexOf(word)===-1)
result.push(word);});});return result;}
function getLanBridgeDevice(){return wordListValue(uci.get('network','lan','device')).find(Boolean)||'br-lan';}
function getBuiltinLanPorts(ports){return chester.ensureArray(ports).filter((port)=>String(port?.role||'').toLowerCase()==='lan').map((port)=>String(port?.device||'').trim()).filter(Boolean);}
function normalizeListValue(value){if(Array.isArray(value))
return value;if(typeof value==='string'){const trimmed=value.trim();if(!trimmed)
return[];if(trimmed.charAt(0)==='['){try{const parsed=JSON.parse(trimmed);if(Array.isArray(parsed))
return parsed;}
catch(err){}}
return[trimmed];}
if(value==null)
return[];return[value];}
function prefixToNetmask(prefix){const prefixLength=parseInt(prefix,10);if(isNaN(prefixLength)||prefixLength<0||prefixLength>32)
return'';const octets=[];let bits=prefixLength;for(let i=0;i<4;i++){const size=Math.max(0,Math.min(8,bits));octets.push(size?String((0xff<<(8-size))&0xff):'0');bits-=size;}
return octets.join('.');}
function parseIpv4Value(value){const raw=normalizeListValue(value).map((entry)=>String(entry||'').trim()).find(Boolean)||'';const match=raw.match(/^([^/]+?)(?:\/(\d{1,2}))?$/);if(!match)
return{address:raw,netmask:''};return{address:match[1],netmask:match[2]?prefixToNetmask(match[2]):''};}
function getInterfaceIpv4(sectionName,fallbackAddress,fallbackNetmask){const primary=parseIpv4Value(uci.get('network',sectionName,'ipaddr'));const parsed=primary.address?primary:parseIpv4Value(uci.get('network',sectionName,'ip4addr'));return{address:parsed.address||fallbackAddress,netmask:uci.get('network',sectionName,'netmask')||parsed.netmask||fallbackNetmask};}
function getInterfaceDevice(sectionName){return normalizeListValue(uci.get('network',sectionName,'device')||uci.get('network',sectionName,'ifname')).map((entry)=>String(entry||'').trim()).find(Boolean)||'';}
function getManagedWanRole(){return(uci.get('chester','network_defaults','wan_role')||'').trim();}
function getOptionalWanMode(){const mode=String(uci.get('chester','network_defaults','optional_wan_mode')||'').trim().toLowerCase();return mode==='lan'?'lan':'wan';}
function getConfiguredOptionalWan(sectionName){const configured=String(uci.get('chester','network_defaults','optional_wan')||uci.get('chester','network_defaults','wan_device')||'').trim();return configured||getInterfaceDevice(sectionName);}
return view.extend({async load(){chester.ensureBaseStyle();await uci.load(['network','dhcp','firewall','chester']);return{ports:(await L.resolveDefault(callBuiltinEthernetPorts(),{result:[]})).result||[]};},renderProtocolFields(proto){if(this.wanAccessField)
this.wanAccessField.hidden=this.wan.mode.value==='lan';if(this.wan.mode.value==='lan'){dom.content(this.wan.dynamicFields,[E('div',{'class':'chester-notice'},[_('The physical WAN port is bridged into LAN. WAN protocol settings are kept but not applied while LAN mode is selected.')])]);return;}
const shared={dhcp:null,pppoe:E('div',{'class':'chester-form-grid'},[chester.createField(_('PPPoE Username'),this.wan.username),chester.createField(_('PPPoE Password'),this.wan.password)]),static:E('div',{'class':'chester-form-grid'},[chester.createField(_('WAN IPv4'),this.wan.ipaddr),chester.createField(_('Subnet Mask'),this.wan.netmask),chester.createField(_('Default Gateway'),this.wan.gateway),chester.createField(_('DNS'),this.wan.dns,_('Separate multiple DNS servers with spaces or commas'))])};const content=shared[proto];dom.content(this.wan.dynamicFields,content?[content]:[]);},ensureWanFirewall(interfaceName){let zone=uci.sections('firewall','zone').find((section)=>section.name==='wan');let zoneSid=zone?.['.name'];if(!zoneSid){zoneSid=uci.add('firewall','zone');uci.set('firewall',zoneSid,'name','wan');uci.set('firewall',zoneSid,'input','REJECT');uci.set('firewall',zoneSid,'output','ACCEPT');uci.set('firewall',zoneSid,'forward','DROP');uci.set('firewall',zoneSid,'masq','1');uci.set('firewall',zoneSid,'mtu_fix','1');}
const networks=arrayValue(uci.get('firewall',zoneSid,'network')).filter(Boolean);if(interfaceName&&networks.indexOf(interfaceName)===-1)
networks.push(interfaceName);uci.set('firewall',zoneSid,'network',networks);const hasForward=uci.sections('firewall','forwarding').some((section)=>section.src==='lan'&&section.dest==='wan');if(!hasForward){const forwardingSid=uci.add('firewall','forwarding');uci.set('firewall',forwardingSid,'src','lan');uci.set('firewall',forwardingSid,'dest','wan');}},removeWanFirewall(interfaceName){uci.sections('firewall','zone').forEach((section)=>{if(section.name!=='wan')
return;const networks=arrayValue(uci.get('firewall',section['.name'],'network')).filter((name)=>name&&name!==interfaceName);if(networks.length)
uci.set('firewall',section['.name'],'network',networks);else
uci.unset('firewall',section['.name'],'network');});},removeWanFromBridge(wanDevice){const lanDevice=getLanBridgeDevice();uci.sections('network','device').forEach((section)=>{if(section.name!==lanDevice)
return;const ports=wordListValue(section.ports).filter((port)=>port!==wanDevice);uci.set('network',section['.name'],'ports',ports);});const lanIfname=wordListValue(uci.get('network','lan','ifname')).filter((port)=>port!==wanDevice);if(lanIfname.length||uci.get('network','lan','ifname'))
uci.set('network','lan','ifname',lanIfname);},addWanToBridge(wanDevice){const lanDevice=getLanBridgeDevice();const fallbackPorts=[...wordListValue(uci.get('network','lan','ifname')),...getBuiltinLanPorts(this.builtinPorts)].filter((port)=>port!==lanDevice);let bridged=false;uci.sections('network','device').forEach((section)=>{if(section.name!==lanDevice)
return;const ports=wordListValue(section.ports);fallbackPorts.forEach((port)=>{if(ports.indexOf(port)===-1)
ports.push(port);});if(ports.indexOf(wanDevice)===-1)
ports.push(wanDevice);uci.set('network',section['.name'],'ports',ports);bridged=true;});if(bridged)
return;const lanIfname=wordListValue(uci.get('network','lan','ifname'));fallbackPorts.forEach((port)=>{if(lanIfname.indexOf(port)===-1)
lanIfname.push(port);});if(lanIfname.indexOf(wanDevice)===-1)
lanIfname.push(wanDevice);uci.set('network','lan','ifname',lanIfname);},async saveChanges(button){chester.setButtonBusy(button,true);try{await uci.load(['network','dhcp','firewall','chester']);const chesterNetwork=chester.ensureNamedSection(uci,'chester','network','network_defaults');const lanSection=chester.ensureNamedSection(uci,'network','interface','lan');const dhcpLan=chester.ensureNamedSection(uci,'dhcp','dhcp','lan');uci.set('network',lanSection,'ipaddr',this.lan.ipaddr.value.trim());uci.set('network',lanSection,'netmask',this.lan.netmask.value.trim());uci.set('dhcp',dhcpLan,'interface','lan');if(this.lan.dhcpEnabled.checked){uci.unset('dhcp',dhcpLan,'ignore');uci.set('dhcp',dhcpLan,'dhcpv4','server');uci.set('dhcp',dhcpLan,'start',this.lan.start.value.trim());uci.set('dhcp',dhcpLan,'limit',this.lan.limit.value.trim());if(!uci.get('dhcp',dhcpLan,'leasetime'))
uci.set('dhcp',dhcpLan,'leasetime','12h');}
else{uci.set('dhcp',dhcpLan,'ignore','1');uci.set('dhcp',dhcpLan,'dhcpv4','disabled');}
if(this.wanAvailable){const wanDevice=this.wanDevice;const proto=this.wan.proto.value;const wanSection=this.wanSectionName;const wanMode=this.wan.mode.value==='lan'?'lan':'wan';if(!wanDevice)
throw new Error(_('No manageable physical WAN port was found'));uci.set('chester',chesterNetwork,'optional_wan',wanDevice);uci.set('chester',chesterNetwork,'optional_wan_mode',wanMode);uci.set('chester',chesterNetwork,'wan_device',wanDevice);uci.set('network',wanSection,'device',wanDevice);uci.set('network',wanSection,'proto',proto);['username','password','ipaddr','netmask','gateway','dns'].forEach((key)=>uci.unset('network',wanSection,key));if(proto==='pppoe'){uci.set('network',wanSection,'username',this.wan.username.value.trim());uci.set('network',wanSection,'password',this.wan.password.value);}
else if(proto==='static'){uci.set('network',wanSection,'ipaddr',this.wan.ipaddr.value.trim());uci.set('network',wanSection,'netmask',this.wan.netmask.value.trim());uci.set('network',wanSection,'gateway',this.wan.gateway.value.trim());const dns=this.wan.dns.value.split(/[\s,]+/).filter(Boolean);if(dns.length)
uci.set('network',wanSection,'dns',dns);}
if(wanMode==='lan'){this.addWanToBridge(wanDevice);uci.set('network',wanSection,'disabled','1');this.removeWanFirewall(wanSection);}
else{this.removeWanFromBridge(wanDevice);uci.unset('network',wanSection,'disabled');this.ensureWanFirewall(wanSection);}}
await chester.saveAndApplyUnchecked(uci);}
catch(err){chester.notify(_('Failed to save network settings: %s').format(err.message||err),'error');}
finally{chester.setButtonBusy(button,false);}},render(data){this.builtinPorts=data.ports||[];this.wanSectionName=getManagedWanRole();const wanSectionExists=!!this.wanSectionName&&!!uci.get('network',this.wanSectionName);this.wanDevice=wanSectionExists?getConfiguredOptionalWan(this.wanSectionName):'';this.wanAvailable=wanSectionExists&&!!this.wanDevice;const proto=this.wanAvailable?(uci.get('network',this.wanSectionName,'proto')||'dhcp'):'dhcp';const wanMode=getOptionalWanMode();const lanIpv4=getInterfaceIpv4('lan','192.168.1.1','255.255.255.0');const wanIpv4=this.wanAvailable?getInterfaceIpv4(this.wanSectionName,'',''):{address:'',netmask:''};const saveButton=chester.createButton(_('Save & Apply'),'chester-button',ui.createHandlerFn(this,function(ev){return this.saveChanges(ev.currentTarget);}));this.lan={ipaddr:E('input',{'class':'chester-input','type':'text','value':lanIpv4.address}),netmask:E('input',{'class':'chester-input','type':'text','value':lanIpv4.netmask}),dhcpEnabled:E('input',{'type':'checkbox','checked':uci.get('dhcp','lan','ignore')==='1'?null:'checked'}),start:E('input',{'class':'chester-input','type':'text','value':uci.get('dhcp','lan','start')||'100'}),limit:E('input',{'class':'chester-input','type':'text','value':uci.get('dhcp','lan','limit')||'150'})};this.wan={mode:E('select',{'class':'chester-select'},[E('option',{'value':'wan','selected':wanMode==='wan'?'selected':null},['WAN']),E('option',{'value':'lan','selected':wanMode==='lan'?'selected':null},['LAN'])]),proto:E('select',{'class':'chester-select'},[E('option',{'value':'dhcp','selected':proto==='dhcp'?'selected':null},['DHCP']),E('option',{'value':'pppoe','selected':proto==='pppoe'?'selected':null},['PPPoE']),E('option',{'value':'static','selected':proto==='static'?'selected':null},[_('Static')])]),username:E('input',{'class':'chester-input','type':'text','value':this.wanAvailable?(uci.get('network',this.wanSectionName,'username')||''):''}),password:E('input',{'class':'chester-input','type':'password','value':this.wanAvailable?(uci.get('network',this.wanSectionName,'password')||''):''}),ipaddr:E('input',{'class':'chester-input','type':'text','value':wanIpv4.address}),netmask:E('input',{'class':'chester-input','type':'text','value':wanIpv4.netmask}),gateway:E('input',{'class':'chester-input','type':'text','value':this.wanAvailable?(uci.get('network',this.wanSectionName,'gateway')||''):''}),dns:E('input',{'class':'chester-input','type':'text','value':this.wanAvailable?arrayValue(uci.get('network',this.wanSectionName,'dns')).join(' '):''}),dynamicFields:E('div')};this.lan.dhcpEnabled.addEventListener('change',()=>{this.lan.start.disabled=!this.lan.dhcpEnabled.checked;this.lan.limit.disabled=!this.lan.dhcpEnabled.checked;});this.lan.dhcpEnabled.dispatchEvent(new Event('change'));this.wan.mode.addEventListener('change',()=>this.renderProtocolFields(this.wan.proto.value));this.wan.proto.addEventListener('change',()=>this.renderProtocolFields(this.wan.proto.value));this.wanAccessField=chester.createField(_('Access Protocol'),E('div',{'class':'chester-select-wrap'},[this.wan.proto]));if(this.wanAvailable)
this.renderProtocolFields(proto);return E('div',{'class':'chester-page'},[E('div',{'class':'chester-shell'},[E('section',{'class':'chester-hero'},[E('div',{'class':'chester-hero__content'},[E('h2',{'class':'chester-hero__title'},[_('Network')]),E('div',{'class':'chester-hero__subtitle'},[_('Manage LAN addressing, DHCP service, and the WAN uplink mode separately.')]),E('div',{'class':'chester-hero__meta'},chester.ensureArray(data.ports).map((port)=>E('span',{'class':'chester-pill'},[E('span',{},[port.device]),E('strong',{},[String(port.role||'').toUpperCase()])])))])]),E('div',{'class':'chester-stack'},[chester.createCard(_('Local LAN'),_('Configure LAN address and DHCP'),[E('div',{'class':'chester-form-grid'},[chester.createField(_('LAN IPv4'),this.lan.ipaddr),chester.createField(_('Subnet Mask'),this.lan.netmask),chester.createField(_('Enable DHCP'),chester.createInlineCheck(this.lan.dhcpEnabled,_('Disable DHCP and keep only the static LAN address'))),E('div'),chester.createField(_('Pool Start'),this.lan.start),chester.createField(_('Pool Size'),this.lan.limit)])]),this.wanAvailable?chester.createCard(_('WAN Config'),_('Configure WAN port and access protocol'),[E('div',{'class':'chester-form-grid'},[chester.createField(_('Port Mode'),E('div',{'class':'chester-select-wrap'},[this.wan.mode])),this.wanAccessField]),this.wan.dynamicFields]):null]),E('div',{'class':'chester-card__footer'},[saveButton])])]);},handleSave:null,handleSaveApply:null,handleReset:null});