'use strict';'require view';'require ui';'require dom';'require uci';'require chester.ui as chester';'require chester.modem as modemHelper';function buildSelect(current,modems){return E('select',{'class':'chester-select'},(modems||[]).map((modem)=>E('option',{'value':modem.id,'selected':modem.id===current?'selected':null},[modemHelper.modemLabel(modem)])));}
function modeTitle(modeKey){switch(String(modeKey||'').toUpperCase()){case'LTE':return _('4G Bands');case'NR':return _('5G Bands');case'1.5G':return _('5G-SA Bands');case'NR_NAS':case'NR_NSA':return _('5G-NSA Bands');case'UMTS':return _('3G Bands');default:return _('%s Bands').format(modeKey);}}
function bandSortValue(value){const match=String(value||'').match(/\d+/);return match?parseInt(match[0],10):Number.MAX_SAFE_INTEGER;}
function normalizeListValue(value){if(Array.isArray(value))
return value;if(value==null||value==='')
return[];return[value];}
function bandLabel(item){const raw=String(item?.band_name||item?.band_id||'');const upper=raw.toUpperCase();if(/LTE/.test(upper)){const match=upper.match(/B(\d+)/);return match?`B${match[1]}`:raw.replace(/^LTE[_\s-]*/i,'');}
if(/NR/.test(upper)){const match=upper.match(/N(\d+)/);return match?`N${match[1]}`:raw.replace(/^(?:SA\s+)?NR[_\s-]*/i,'');}
if(/UMTS/.test(upper)){const match=upper.match(/(\d+)/);return match?`${match[1]}`:raw.replace(/^UMTS[_\s-]*/i,'');}
return raw;}
function createBandCheckbox(name,value,label,checked){const input=E('input',{'type':'checkbox','name':name,'value':value,'checked':checked?'checked':null});const node=E('label',{'class':`chester-band-option${checked ? ' is-active' : ''}`},[input,E('span',{'class':'chester-band-option__mark','aria-hidden':'true'}),E('span',{'class':'chester-band-option__label'},[label])]);input.addEventListener('change',()=>{node.classList.toggle('is-active',input.checked);});return node;}
return view.extend({load(){chester.ensureBaseStyle();return Promise.all([modemHelper.loadModems(),uci.load('chester')]);},renderSelector(){if(this.modems.length<=1){dom.content(this.selectorNode,[]);return;}
const select=buildSelect(this.selectedModemId,this.modems);select.addEventListener('change',(ev)=>{this.selectedModemId=ev.currentTarget.value;this.refresh();});dom.content(this.selectorNode,[chester.createCard(_('Current Modem'),null,[chester.createField(_('Select Modem'),E('div',{'class':'chester-select-wrap'},[select]))])]);},createShell(){this.selectorNode=E('div');this.bodyNode=E('div');return E('div',{'class':'chester-page'},[E('div',{'class':'chester-shell'},[this.selectorNode,this.bodyNode])]);},createIntroCard(){return chester.createCard(null,null,[E('div',{'class':'chester-lockband-intro'},[E('div',{'class':'chester-lockband-intro__icon','aria-hidden':'true'},['||']),E('div',{'class':'chester-lockband-intro__body'},[E('div',{'class':'chester-lockband-intro__title'},[_('Band Lock Mode')]),E('div',{'class':'chester-lockband-intro__desc'},[_('This feature is intended for network debugging. Incorrect changes may prevent network access and might require a factory reset.')])])])]);},createModeCard(modemId,modeKey,modeInfo){const available=(modeInfo?.available_band||[]).slice().sort((a,b)=>bandSortValue(a.band_id||a.band_name)-bandSortValue(b.band_id||b.band_name));const locked=modeInfo?.lock_band||[];const choices=available.map((item)=>createBandCheckbox(`lockband_${modemId}_${modeKey}`,item.band_id,bandLabel(item),locked.indexOf(item.band_id)!==-1));const listNode=E('div',{'class':'chester-band-list'},choices);const button=chester.createButton(_('Save'),'chester-button',ui.createHandlerFn(this,async(ev)=>{const selected=choices.filter((node)=>node.querySelector('input').checked).map((node)=>node.querySelector('input').value).sort((a,b)=>bandSortValue(a)-bandSortValue(b));chester.setButtonBusy(ev.currentTarget,true);try{await modemHelper.setLockBand(modemId,{band_class:modeKey,lock_band:selected.join(',')});chester.notify(_('Band lock settings sent for %s').format(modeKey),'info');}
catch(err){chester.notify(_('Failed to update band lock settings for %s: %s').format(modeKey,err.message||err),'error');}
finally{chester.setButtonBusy(ev.currentTarget,false);}}));return chester.createCard(modeTitle(modeKey),null,[available.length?listNode:chester.createEmptyState(_('No selectable bands are available for this mode')),E('div',{'class':'chester-card__footer chester-lockband-card__footer'},[button])]);},async refresh(){const modem=this.modems.find((item)=>item.id===this.selectedModemId)||this.modems[0];if(!modem){dom.content(this.bodyNode,[chester.createEmptyState(_('No configurable modem detected'))]);return;}
dom.content(this.bodyNode,[E('div',{'class':'chester-loading'},[_('Reading band lock configuration...')])]);try{const[disabled,bandInfo]=await Promise.all([modemHelper.getDisabledFeatures(modem.id),modemHelper.getLockBand(modem.id)]);const disabledFeatures=disabled.disabled_features||[];if(disabledFeatures.indexOf('LockBand')!==-1){dom.content(this.bodyNode,[chester.createEmptyState(_('Band locking is not supported by the current modem'))]);return;}
const lockband=bandInfo.lockband||{};const hiddenModes=normalizeListValue(uci.get('chester','modem_cfg_main','hidden_lockband')).map((mode)=>String(mode||'').toUpperCase());const modeKeys=Object.keys(lockband).filter((modeKey)=>(lockband[modeKey]?.available_band||[]).length||(lockband[modeKey]?.lock_band||[]).length).filter((modeKey)=>hiddenModes.indexOf(String(modeKey||'').toUpperCase())===-1).sort((a,b)=>{const order={UMTS:1,LTE:2,NR:3};return(order[String(a).toUpperCase()]||99)-(order[String(b).toUpperCase()]||99);});dom.content(this.bodyNode,[this.createIntroCard(),modeKeys.length?E('div',{'class':'chester-lockband-grid'},modeKeys.map((modeKey)=>this.createModeCard(modem.id,modeKey,lockband[modeKey]))):chester.createEmptyState(_('No selectable bands are available for the current modem'))]);}
catch(err){dom.content(this.bodyNode,[chester.createEmptyState(_('Failed to load band lock settings'),err.message||err)]);}},render(data){const modems=Array.isArray(data)&&Array.isArray(data[0])?data[0]:data;this.modems=modems||[];this.selectedModemId=modemHelper.ensureSelected(this.selectedModemId,this.modems);const shell=this.createShell();this.renderSelector();this.refresh();return shell;},handleSave:null,handleSaveApply:null,handleReset:null});