'use strict';'require view';'require ui';'require uci';'require dom';'require rpc';'require chester.ui as chester';'require chester.modem as modemHelper';const PAGE_SIZE=8;const AUTH_CHOICES=[{value:'none',label:_('No Authentication')},{value:'MsChapV2',label:'MsChapV2'},{value:'pap',label:_('PAP')},{value:'chap',label:_('CHAP')},{value:'both',label:_('PAP / CHAP')}];const PDP_CHOICES=[{value:'ip',label:_('IPv4')},{value:'ipv6',label:_('IPv6')},{value:'ipv4v6',label:_('IPv4 / IPv6')}];const callApnPresetsGet=rpc.declare({object:'chester',method:'apn_presets_get',expect:{}});const callApnPresetsSave=rpc.declare({object:'chester',method:'apn_presets_save',params:['presets'],expect:{}});const callApnPresetsReset=rpc.declare({object:'chester',method:'apn_presets_reset',expect:{}});function textValue(value){return value==null?'':String(value);}
function normalizeAuth(value){return textValue(value)||'none';}
function normalizePdp(value){return textValue(value)||'ipv4v6';}
function sameConfig(left,right){return textValue(left.apn)===textValue(right.apn)&&normalizeAuth(left.auth)===normalizeAuth(right.auth)&&textValue(left.username)===textValue(right.username)&&textValue(left.password)===textValue(right.password);}
function findPresetIndexForConfig(presets,config){return(presets||[]).findIndex((preset)=>sameConfig(preset,config));}
function isEmptyConfig(config){return!textValue(config.apn)&&!textValue(config.username)&&!textValue(config.password)&&!textValue(config.auth);}
function normalizePreset(preset,indexValue){const operator=textValue(preset?.operator).trim()||`apn${indexValue + 1}`;let id=textValue(preset?.id).trim()||operator.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');if(!id)
id=`apn-${indexValue + 1}`;return{id,operator,apn:textValue(preset?.apn).trim(),auth:normalizeAuth(preset?.auth),username:textValue(preset?.username).trim(),password:textValue(preset?.password)};}
function configFromSection(modem,slot){if(slot==='2'){return{apn:uci.get('qmodem',modem.id,'apn2')||'',auth:uci.get('qmodem',modem.id,'auth2')||'',username:uci.get('qmodem',modem.id,'username2')||'',password:uci.get('qmodem',modem.id,'password2')||''};}
return{apn:uci.get('qmodem',modem.id,'apn')||'',auth:uci.get('qmodem',modem.id,'auth')||'',username:uci.get('qmodem',modem.id,'username')||'',password:uci.get('qmodem',modem.id,'password')||''};}
function nextUserConfigName(presets){const used={};(presets||[]).forEach((preset)=>{const match=textValue(preset.operator).match(/^userconfig-(\d+)$/i);if(match)
used[parseInt(match[1],10)]=true;});for(let i=1;i<10000;i++){if(!used[i])
return`userconfig-${i}`;}
return`userconfig-${(presets || []).length + 1}`;}
function syncPresetsWithModems(presets,modems){let changed=false;const result=(presets||[]).map((preset,indexValue)=>normalizePreset(preset,indexValue));(modems||[]).forEach((modem)=>{[configFromSection(modem,'1'),configFromSection(modem,'2')].forEach((config)=>{if(findPresetIndexForConfig(result,config)>=0)
return;if(isEmptyConfig(config)){result.push(normalizePreset({id:'empty-apn',operator:'Empty APN',apn:'',auth:'none',username:'',password:''},result.length));changed=true;return;}
const name=nextUserConfigName(result);result.push(normalizePreset(Object.assign({},config,{id:name,operator:name}),result.length));changed=true;});});return{presets:result,changed};}
function buildSelect(currentValue,choices){return E('select',{'class':'chester-select'},(choices||[]).map((choice)=>E('option',{'value':choice.value,'selected':choice.value===currentValue?'selected':null},[choice.label])));}
function tableCell(label,content){const children=Array.isArray(content)?content:[content];return E('td',{'data-title':label},children.filter((item)=>item!=null&&item!==false).map((item)=>item==null?'':item));}
function rowKey(row){return`preset:${row.index}`;}
return view.extend({async load(){chester.ensureBaseStyle();await uci.load(['qmodem','chester']);const presetData=await L.resolveDefault(callApnPresetsGet(),{country:'unknown',presets:[]});const modems=modemHelper.getModemSections();const synced=syncPresetsWithModems(presetData.presets,modems);if(synced.changed){await L.resolveDefault(callApnPresetsSave(synced.presets),null);presetData.presets=synced.presets;}
return{modems,presetData};},getCurrentModem(){return this.modems.find((item)=>item.id===this.selectedModemId)||this.modems[0]||null;},setPresetData(data){this.country=data?.country||'unknown';this.presets=chester.ensureArray(data?.presets).map((preset,indexValue)=>normalizePreset(preset,indexValue));},applySavedPresetData(data,fallbackPresets){if(data?.result===false)
throw new Error(data.message||_('Failed to save APN presets'));if(Array.isArray(data?.presets)){this.setPresetData(data);return;}
this.presets=chester.ensureArray(fallbackPresets).map((preset,indexValue)=>normalizePreset(preset,indexValue));if(data?.country)
this.country=data.country;},getActiveLabel(modem,preset){const sim1=configFromSection(modem,'1');const sim2=configFromSection(modem,'2');const labels=[];if(isEmptyConfig(sim2)||sameConfig(sim1,sim2))
return sameConfig(preset,sim1)?'SIM1/2':'';if(sameConfig(preset,sim1))
labels.push('SIM1');if(sameConfig(preset,sim2))
labels.push('SIM2');return labels.join(', ');},nextPresetName(){const used={};this.presets.forEach((preset)=>{const match=preset.operator.match(/^apn(\d+)$/i);if(match)
used[parseInt(match[1],10)]=true;});for(let i=1;i<10000;i++){if(!used[i])
return`apn${i}`;}
return`apn${this.presets.length + 1}`;},makePresetId(operator){let base=textValue(operator).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||'apn';let candidate=base;let suffix=1;const used={};this.presets.forEach((preset)=>{used[preset.id]=true;});while(used[candidate]){suffix++;candidate=`${base}-${suffix}`;}
return candidate;},ensurePresetForConfig(config){const draft=normalizePreset(Object.assign({},config,{operator:textValue(config.operator).trim()||nextUserConfigName(this.presets)}),this.presets.length);const idIndex=this.presets.findIndex((preset)=>preset.id&&preset.id===draft.id);const sameIndex=this.presets.findIndex((preset)=>sameConfig(preset,draft));if(idIndex>=0){this.presets[idIndex]=normalizePreset(Object.assign({},draft,{id:this.presets[idIndex].id}),idIndex);return;}
if(sameIndex>=0)
return;draft.id=this.makePresetId(draft.operator);this.presets.push(draft);},writeConfigToUci(modemId,target,config){const slots=target==='both'?['1','2']:[target];slots.forEach((slot)=>{const suffix=slot==='2'?'2':'';uci.set('qmodem',modemId,`apn${suffix}`,textValue(config.apn).trim());uci.set('qmodem',modemId,`auth${suffix}`,normalizeAuth(config.auth));if(normalizeAuth(config.auth)==='none'){uci.unset('qmodem',modemId,`username${suffix}`);uci.unset('qmodem',modemId,`password${suffix}`);}
else{uci.set('qmodem',modemId,`username${suffix}`,textValue(config.username).trim());uci.set('qmodem',modemId,`password${suffix}`,textValue(config.password));}});},async applyPdpType(value,button){const modem=this.getCurrentModem();if(!modem)
return;chester.setButtonBusy(button,true);try{await uci.load('qmodem');uci.set('qmodem',modem.id,'pdp_type',normalizePdp(value));await chester.saveAndApplyUnchecked(uci);await modemHelper.restartDialService();chester.notify(_('PDP Type saved. Dial service restarted.'),'info');window.setTimeout(()=>window.location.reload(),300);}
catch(err){chester.notify(_('Failed to save PDP Type: %s').format(err.message||err),'error');}
finally{chester.setButtonBusy(button,false);}},renderPdpTypeCard(){const modem=this.getCurrentModem();if(!modem)
return chester.createCard(_('PDP Type'),null,[chester.createEmptyState(_('No configurable modem detected'))]);const pdpSelect=buildSelect(normalizePdp(uci.get('qmodem',modem.id,'pdp_type')),PDP_CHOICES);return chester.createCard(_('PDP Type'),_('SIM1 and SIM2 share this setting.'),[E('div',{'class':'chester-form-grid'},[chester.createField(_('PDP Type'),E('div',{'class':'chester-select-wrap'},[pdpSelect]))]),E('div',{'class':'chester-card__footer','style':'justify-content:flex-start;gap:12px;flex-wrap:wrap'},[chester.createButton(_('Save PDP Type'),'chester-button',ui.createHandlerFn(this,function(ev){return this.applyPdpType(pdpSelect.value,ev.currentTarget);}))])]);},async applyConfig(target,config,button){const modem=this.getCurrentModem();if(!modem)
return;chester.setButtonBusy(button,true);try{await uci.load('qmodem');this.writeConfigToUci(modem.id,target,config);this.ensurePresetForConfig(config);await callApnPresetsSave(this.presets);await chester.saveAndApplyUnchecked(uci);await modemHelper.restartDialService();chester.notify(_('APN settings saved. Dial service restarted.'),'info');window.setTimeout(()=>window.location.reload(),300);}
catch(err){chester.notify(_('Failed to save APN settings: %s').format(err.message||err),'error');}
finally{chester.setButtonBusy(button,false);}},async savePresets(button){chester.setButtonBusy(button,true);try{const nextPresets=this.presets.map((preset,indexValue)=>normalizePreset(preset,indexValue));const result=await callApnPresetsSave(nextPresets);this.applySavedPresetData(result,nextPresets);this.renderTable();chester.notify(_('APN presets saved.'),'info');}
catch(err){chester.notify(_('Failed to save APN presets: %s').format(err.message||err),'error');}
finally{chester.setButtonBusy(button,false);}},async resetPresets(button){if(!window.confirm(_('Restore the preset APN list for the current country?')))
return;chester.setButtonBusy(button,true);try{const result=await callApnPresetsReset();if(result?.result===false)
throw new Error(result.message||_('Failed to restore APN presets'));this.setPresetData(result);this.page=0;this.renderTable();chester.notify(_('APN presets restored.'),'info');}
catch(err){chester.notify(_('Failed to restore APN presets: %s').format(err.message||err),'error');}
finally{chester.setButtonBusy(button,false);}},openPresetModal(indexValue){const isNew=indexValue==null;const source=isNew?normalizePreset({operator:this.nextPresetName()},this.presets.length):normalizePreset(this.presets[indexValue],indexValue);if(isNew)
source.id=this.makePresetId(source.operator);this.openConfigModal(isNew?_('Add APN Preset'):_('Edit APN Preset'),source,async(config,ev)=>{const button=ev?.currentTarget;chester.setButtonBusy(button,true);try{let nextPresets=this.presets.map((preset,presetIndex)=>normalizePreset(preset,presetIndex));if(isNew){config.id=this.makePresetId(config.operator);nextPresets.push(normalizePreset(config,nextPresets.length));}
else{nextPresets[indexValue]=normalizePreset(config,indexValue);}
const result=await callApnPresetsSave(nextPresets);this.applySavedPresetData(result,nextPresets);ui.hideModal();this.renderTable();chester.notify(_('APN presets saved.'),'info');}
catch(err){chester.notify(_('Failed to save APN presets: %s').format(err.message||err),'error');}
finally{chester.setButtonBusy(button,false);}});},openConfigModal(title,config,onSave){const operatorInput=E('input',{'class':'chester-input','type':'text','value':textValue(config.operator)});const apnInput=E('input',{'class':'chester-input','type':'text','value':textValue(config.apn)});const authSelect=buildSelect(normalizeAuth(config.auth),AUTH_CHOICES);const usernameInput=E('input',{'class':'chester-input','type':'text','value':textValue(config.username)});const passwordInput=E('input',{'class':'chester-input','type':'password','value':textValue(config.password)});const syncAuth=()=>{const disabled=authSelect.value==='none';usernameInput.disabled=disabled;passwordInput.disabled=disabled;};const saveButton=chester.createButton(_('Save'),'chester-button',ui.createHandlerFn(this,async function(ev){await onSave({id:config.id,operator:operatorInput.value.trim(),apn:apnInput.value.trim(),auth:authSelect.value,username:usernameInput.value.trim(),password:passwordInput.value},ev);}));authSelect.addEventListener('change',syncAuth);syncAuth();ui.showModal(title,[E('div',{'class':'chester-card'},[E('div',{'class':'chester-form-grid'},[chester.createField(_('Operator'),operatorInput),chester.createField(_('APN'),apnInput),chester.createField(_('Authentication'),E('div',{'class':'chester-select-wrap'},[authSelect])),chester.createField(_('Username'),usernameInput),chester.createField(_('Password'),passwordInput)]),E('div',{'class':'chester-card__footer'},[chester.createButton(_('Cancel'),'chester-button-secondary',()=>ui.hideModal()),saveButton])])],'cbi-modal');},removePreset(indexValue){if(!window.confirm(_('Delete this APN preset?')))
return;this.presets.splice(indexValue,1);this.selectedRowKey=null;if(this.page>0&&this.page*PAGE_SIZE>=this.presets.length)
this.page--;this.renderTable();},selectRow(key){this.selectedRowKey=key;this.renderTable();},getSelectedRow(rows){return(rows||[]).find((row)=>rowKey(row)===this.selectedRowKey)||null;},renderSelectorCell(row){const key=rowKey(row);return tableCell(_('Select'),E('input',{'type':'checkbox','checked':this.selectedRowKey===key?'checked':null,'change':()=>this.selectRow(this.selectedRowKey===key?null:key)}));},renderConfigCells(config){return[tableCell(_('APN'),textValue(config.apn)),tableCell(_('Authentication'),textValue(config.auth)),tableCell(_('Username'),textValue(config.username)),tableCell(_('Password'),textValue(config.password))];},renderPresetRow(preset,absoluteIndex,activeLabel){const row={type:'preset',index:absoluteIndex,preset};return E('tr',{},[this.renderSelectorCell(row),tableCell(_('Name'),preset.operator),tableCell(_('Active SIM'),activeLabel),tableCell(_('Operator'),preset.operator),...this.renderConfigCells(preset)]);},renderPagination(totalPages){const prev=chester.createButton(_('Previous'),'chester-button-secondary',()=>{this.page=Math.max(0,this.page-1);this.renderTable();},{'disabled':this.page<=0?'disabled':null});const next=chester.createButton(_('Next'),'chester-button-secondary',()=>{this.page=Math.min(totalPages-1,this.page+1);this.renderTable();},{'disabled':this.page>=totalPages-1?'disabled':null});return E('div',{'class':'chester-card__footer','style':'justify-content:space-between;flex-wrap:wrap'},[E('span',{'class':'chester-card__desc'},[_('Page %d of %d').format(this.page+1,totalPages)]),E('div',{'class':'chester-card__actions'},[prev,next])]);},renderSelectedActions(rows){const row=this.getSelectedRow(rows);const selectedActions=[];const globalActions=[chester.createButton(_('Add APN Preset'),'chester-button',()=>this.openPresetModal(null)),chester.createButton(_('Save Presets'),'chester-button-secondary',ui.createHandlerFn(this,function(ev){return this.savePresets(ev.currentTarget);})),chester.createButton(_('Restore Defaults'),'chester-button-secondary',ui.createHandlerFn(this,function(ev){return this.resetPresets(ev.currentTarget);}))];if(row?.type==='preset'){const preset=row.preset;selectedActions.push(chester.createButton(_('Apply Selected to SIM1'),'chester-button-secondary',ui.createHandlerFn(this,function(ev){return this.applyConfig('1',preset,ev.currentTarget);})),chester.createButton(_('Apply Selected to SIM2'),'chester-button-secondary',ui.createHandlerFn(this,function(ev){return this.applyConfig('2',preset,ev.currentTarget);})),chester.createButton(_('Apply Selected to SIM1/2'),'chester-button-secondary',ui.createHandlerFn(this,function(ev){return this.applyConfig('both',preset,ev.currentTarget);})),chester.createButton(_('Edit Selected'),'chester-button-secondary',()=>this.openPresetModal(row.index)),chester.createButton(_('Delete Selected'),'chester-button-secondary',()=>this.removePreset(row.index)));}
return E('div',{'class':'chester-apn-actions'},chester.compactNodes([selectedActions.length?E('div',{'class':'chester-card__footer','style':'justify-content:flex-start;gap:12px;flex-wrap:wrap'},selectedActions):null,E('div',{'class':'chester-card__footer','style':'justify-content:flex-start;gap:12px;flex-wrap:wrap'},globalActions)]));},renderTable(){const modem=this.getCurrentModem();if(!modem){dom.content(this.tableNode,[chester.createEmptyState(_('No configurable modem detected'))]);return;}
const totalPages=Math.max(1,Math.ceil(this.presets.length/PAGE_SIZE));if(this.page>=totalPages)
this.page=totalPages-1;const offset=this.page*PAGE_SIZE;const presetRows=this.presets.slice(offset,offset+PAGE_SIZE).map((preset,indexValue)=>this.renderPresetRow(preset,offset+indexValue,this.getActiveLabel(modem,preset)));const actionRows=this.presets.map((preset,indexValue)=>({type:'preset',index:indexValue,preset}));dom.content(this.tableNode,chester.compactNodes([E('div',{'class':'chester-device-table-wrap'},[E('table',{'class':'chester-table chester-device-table chester-apn-table'},[E('thead',{},[E('tr',{},[E('th',{},[_('Select')]),E('th',{},[_('Name')]),E('th',{},[_('Active SIM')]),E('th',{},[_('Operator')]),E('th',{},[_('APN')]),E('th',{},[_('Authentication')]),E('th',{},[_('Username')]),E('th',{},[_('Password')])])]),E('tbody',{},presetRows.length?presetRows:[E('tr',{},[E('td',{'colspan':'8'},[chester.createEmptyState(_('No APN presets configured'))])])])])]),this.renderSelectedActions(actionRows),this.presets.length>PAGE_SIZE?this.renderPagination(totalPages):null]));},render(data){this.modems=data.modems||[];this.selectedModemId=modemHelper.ensureSelected(this.selectedModemId,this.modems);this.page=this.page||0;this.setPresetData(data.presetData);this.tableNode=E('div');this.pdpTypeNode=E('div');const selector=buildSelect(this.selectedModemId,this.modems.map((modem)=>({value:modem.id,label:modemHelper.modemLabel(modem)})));selector.addEventListener('change',()=>{this.selectedModemId=selector.value;this.renderTable();dom.content(this.pdpTypeNode,[this.renderPdpTypeCard()]);});window.setTimeout(()=>this.renderTable(),0);dom.content(this.pdpTypeNode,[this.renderPdpTypeCard()]);return E('div',{'class':'chester-page'},[E('div',{'class':'chester-shell'},[E('section',{'class':'chester-hero'},[E('div',{'class':'chester-hero__content'},[E('h2',{'class':'chester-hero__title'},[_('APN Config')]),E('div',{'class':'chester-hero__subtitle'},[_('Manage SIM APN settings and country preset lists.')]),E('div',{'class':'chester-hero__meta'},[E('span',{'class':'chester-pill'},[E('span',{},[_('APN Country')]),E('strong',{},[this.country])])])])]),...chester.compactNodes([this.modems.length>1?chester.createCard(_('Current Modem'),null,[chester.createField(_('Select Modem'),E('div',{'class':'chester-select-wrap'},[selector]))]):null,this.pdpTypeNode,chester.createCard(_('APN List'),null,[this.tableNode])])])]);},handleSave:null,handleSaveApply:null,handleReset:null});